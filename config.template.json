{
  "poll_interval": 30,
  "max_tokens_per_hour": 200000,
  "max_calls_per_hour": 20,
  "cb_no_progress_threshold": 3,
  "cb_cooldown_minutes": 30,
  "default_model": "sonnet",
  "triage_model": "haiku",
  "max_task_attempts": 3,

  "email": {
    "recipients": "you@example.com",
    "send_command": "resend-email -m \"$BODY\" \"$RECIPIENTS\" \"$SUBJECT\""
  },

  "searxng": {
    "enabled": false,
    "url": "http://localhost:8889",
    "ssh_host": ""
  },

  "fleet": {
    "enabled": false,
    "health_dest": "your-homelab-hostname",
    "signal_hosts": [],
    "machines": {}
  },

  "scan": {
    "enabled": true,
    "model": "claude-opus-4-6",
    "max_budget_usd": 2.00,
    "max_turns": 10
  },

  "litellm": {
    "enabled": false,
    "_comment": "Route claude -p through LiteLLM to any supported model (Gemini, Groq, etc). Dramatically cheaper than direct Anthropic calls. Requires LiteLLM server with Anthropic-compatible /v1/messages endpoint.",
    "url": "http://localhost:4000",
    "key": "sk-your-litellm-master-key",
    "model": "gemini-3-flash"
  }
}
